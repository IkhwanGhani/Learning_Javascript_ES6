window.onload = function() {
  //example 1:

  var temp = `hello, my name is ikhwan`;

  console.log(temp);

  // example 2

  function logNinja(name, age) {
    console.log(`my name is ${name} 
  and my age is ${age + 9}`);
  }

  logNinja("Ikhwan", 29);
};
